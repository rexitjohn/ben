# Architecture

```text
structured input
      ↓
schema / QC
      ↓
preprocessing
      ↓
classifier
      ↓
probability score
      ↓
evaluation
```

Alpha 1.0 deliberately favors a simple baseline so leakage and preprocessing errors are easier to inspect.
