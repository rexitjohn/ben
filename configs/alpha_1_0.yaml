experiment:
  name: alpha_1_0
  seed: 42
  test_size: 0.25

data:
  rows: 1000
  positive_rate: 0.50

model:
  type: logistic_regression
  C: 1.0
  max_iter: 2000
  class_weight: balanced

evaluation:
  threshold: 0.50

output:
  directory: artifacts/alpha_1_0
